[build]
  functions = "netlify/functions"
  publish = "."

# This redirect lets your frontend talk to /api/chat instead of the long .netlify/functions path!
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
